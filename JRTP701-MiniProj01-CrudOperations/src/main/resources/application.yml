# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: JRTP701-MiniProj01-CrudOperations
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:shani
    username: admin
    password: admin
  jpa:
    show-sql: true
server:
  port: 4044
